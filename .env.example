# Frontend -> backend base URL (include the stage path)
# Local dev (serverless-offline):
#   VITE_BACKEND_URL=http://localhost:3000/dev
# Production (example):
#   VITE_BACKEND_URL=https://<api-id>.execute-api.<region>.amazonaws.com/prod
VITE_BACKEND_URL=
